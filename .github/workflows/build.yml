name: Build/release

on: push

jobs:
  release:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [macos-latest, windows-latest]

    steps:
      - name: Check out Git repository
        uses: actions/checkout@v1

      - name: Install Node.js, NPM and Yarn
        uses: actions/setup-node@v1
        with:
          node-version: 16.x

      - name: install
        run: |
          yarn install
        env:
          GH_TOKEN: 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

      - name: build
        run: |
          yarn build
        env:
          GH_TOKEN: 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
